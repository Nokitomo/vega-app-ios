diff --git a/node_modules/@8man/react-native-media-console/lib/commonjs/VideoPlayer.js b/node_modules/@8man/react-native-media-console/lib/commonjs/VideoPlayer.js
index 13dd30b..c941925 100644
--- a/node_modules/@8man/react-native-media-console/lib/commonjs/VideoPlayer.js
+++ b/node_modules/@8man/react-native-media-console/lib/commonjs/VideoPlayer.js
@@ -104,6 +104,7 @@ const AnimatedVideoPlayer = props => {
   const [buffering, setBuffering] = (0, _react.useState)(false);
   const [cachedDuration, setCachedDuration] = (0, _react.useState)(0);
   const [cachedPosition, setCachedPosition] = (0, _react.useState)(0);
+  const endedRef = (0, _react.useRef)(false);
   const videoRef = props.videoRef || _videoRef;
   const {
     clearControlTimeout,
@@ -145,6 +146,7 @@ const AnimatedVideoPlayer = props => {
     }
   }, [seeking, setControlTimeout, onSeek, duration]);
   const _onEnd = (0, _react.useCallback)(() => {
+    endedRef.current = true;
     if (currentTime < duration) {
       setCurrentTime(duration);
       setPaused(!props.repeat);
@@ -156,6 +158,12 @@ const AnimatedVideoPlayer = props => {
       onEnd();
     }
   }, [currentTime, duration, props.repeat, showOnEnd, onEnd]);
+  const isNearEnd = (0, _react.useCallback)((time, totalDuration) => {
+    if (!(totalDuration > 0)) {
+      return false;
+    }
+    return time >= totalDuration - 0.25;
+  }, []);
   const _onError = (0, _react.useCallback)(() => {
     setError(true);
     setLoading(false);
@@ -167,6 +175,7 @@ const AnimatedVideoPlayer = props => {
     }
   }, [onLoadStart]);
   const _onLoad = (0, _react.useCallback)(data => {
+    endedRef.current = false;
     setDuration(data.duration);
     setLoading(false);
     if (showControls) {
@@ -231,11 +240,11 @@ const AnimatedVideoPlayer = props => {
       setTimeout(() => {
         !buffering && setPaused(true);
       });
-    } else {
+    } else if (!endedRef.current && !isNearEnd(currentTime, duration)) {
       setPaused(false);
     }
     console.log(playBack);
-  }, [buffering]);
+  }, [buffering, currentTime, duration, isNearEnd]);
   const events = (0, _react.useMemo)(() => ({
     onError: onError || _onError,
     onBack: onBack || (0, _utils._onBack)(navigator),
@@ -336,11 +345,10 @@ const AnimatedVideoPlayer = props => {
     inverted: invertedPan
   });
   (0, _react.useEffect)(() => {
-    if (currentTime >= duration && duration > 0) {
-      var _videoRef$current4;
-      videoRef === null || videoRef === void 0 || (_videoRef$current4 = videoRef.current) === null || _videoRef$current4 === void 0 || _videoRef$current4.seek(0);
+    if (!isNearEnd(currentTime, duration) && endedRef.current) {
+      endedRef.current = false;
     }
-  }, [currentTime, duration, videoRef]);
+  }, [currentTime, duration, isNearEnd]);
   (0, _react.useEffect)(() => {
     if (toggleResizeModeOnFullscreen) {
       setResizeMode(_isFullscreen ? _reactNativeVideo.ResizeMode.CONTAIN : _reactNativeVideo.ResizeMode.COVER);
@@ -486,10 +494,10 @@ const AnimatedVideoPlayer = props => {
   // Memoize onBuffer callback
   const onBuffer = (0, _react.useCallback)(e => {
     setBuffering(e.isBuffering);
-    if (!e.isBuffering) {
+    if (!e.isBuffering && !endedRef.current && !isNearEnd(currentTime, duration)) {
       setPaused(false);
     }
-  }, []);
+  }, [currentTime, duration, isNearEnd]);
 
   // Memoize source URI for dependency comparison - use deep comparison for stability
   const sourceUri = (0, _react.useMemo)(() => {
@@ -511,6 +519,7 @@ const AnimatedVideoPlayer = props => {
   // reset on url change - only reset if source actually changed and component has initialized
   (0, _react.useEffect)(() => {
     if (hasInitialized.current && sourceUri !== prevSourceUri.current && sourceUri !== null) {
+      endedRef.current = false;
       prevSourceUri.current = sourceUri;
       setLoading(true);
       setSeekerFillWidth(0);
diff --git a/node_modules/@8man/react-native-media-console/lib/module/VideoPlayer.js b/node_modules/@8man/react-native-media-console/lib/module/VideoPlayer.js
index 8d3daca..597ca65 100644
--- a/node_modules/@8man/react-native-media-console/lib/module/VideoPlayer.js
+++ b/node_modules/@8man/react-native-media-console/lib/module/VideoPlayer.js
@@ -96,6 +96,7 @@ const AnimatedVideoPlayer = props => {
   const [buffering, setBuffering] = useState(false);
   const [cachedDuration, setCachedDuration] = useState(0);
   const [cachedPosition, setCachedPosition] = useState(0);
+  const endedRef = useRef(false);
   const videoRef = props.videoRef || _videoRef;
   const {
     clearControlTimeout,
@@ -137,6 +138,7 @@ const AnimatedVideoPlayer = props => {
     }
   }, [seeking, setControlTimeout, onSeek, duration]);
   const _onEnd = useCallback(() => {
+    endedRef.current = true;
     if (currentTime < duration) {
       setCurrentTime(duration);
       setPaused(!props.repeat);
@@ -148,6 +150,12 @@ const AnimatedVideoPlayer = props => {
       onEnd();
     }
   }, [currentTime, duration, props.repeat, showOnEnd, onEnd]);
+  const isNearEnd = useCallback((time, totalDuration) => {
+    if (!(totalDuration > 0)) {
+      return false;
+    }
+    return time >= totalDuration - 0.25;
+  }, []);
   const _onError = useCallback(() => {
     setError(true);
     setLoading(false);
@@ -159,6 +167,7 @@ const AnimatedVideoPlayer = props => {
     }
   }, [onLoadStart]);
   const _onLoad = useCallback(data => {
+    endedRef.current = false;
     setDuration(data.duration);
     setLoading(false);
     if (showControls) {
@@ -223,11 +232,11 @@ const AnimatedVideoPlayer = props => {
       setTimeout(() => {
         !buffering && setPaused(true);
       });
-    } else {
+    } else if (!endedRef.current && !isNearEnd(currentTime, duration)) {
       setPaused(false);
     }
     console.log(playBack);
-  }, [buffering]);
+  }, [buffering, currentTime, duration, isNearEnd]);
   const events = useMemo(() => ({
     onError: onError || _onError,
     onBack: onBack || _onBack(navigator),
@@ -328,11 +337,10 @@ const AnimatedVideoPlayer = props => {
     inverted: invertedPan
   });
   useEffect(() => {
-    if (currentTime >= duration && duration > 0) {
-      var _videoRef$current4;
-      videoRef === null || videoRef === void 0 || (_videoRef$current4 = videoRef.current) === null || _videoRef$current4 === void 0 || _videoRef$current4.seek(0);
+    if (!isNearEnd(currentTime, duration) && endedRef.current) {
+      endedRef.current = false;
     }
-  }, [currentTime, duration, videoRef]);
+  }, [currentTime, duration, isNearEnd]);
   useEffect(() => {
     if (toggleResizeModeOnFullscreen) {
       setResizeMode(_isFullscreen ? ResizeMode.CONTAIN : ResizeMode.COVER);
@@ -478,10 +486,10 @@ const AnimatedVideoPlayer = props => {
   // Memoize onBuffer callback
   const onBuffer = useCallback(e => {
     setBuffering(e.isBuffering);
-    if (!e.isBuffering) {
+    if (!e.isBuffering && !endedRef.current && !isNearEnd(currentTime, duration)) {
       setPaused(false);
     }
-  }, []);
+  }, [currentTime, duration, isNearEnd]);
 
   // Memoize source URI for dependency comparison - use deep comparison for stability
   const sourceUri = useMemo(() => {
@@ -503,6 +511,7 @@ const AnimatedVideoPlayer = props => {
   // reset on url change - only reset if source actually changed and component has initialized
   useEffect(() => {
     if (hasInitialized.current && sourceUri !== prevSourceUri.current && sourceUri !== null) {
+      endedRef.current = false;
       prevSourceUri.current = sourceUri;
       setLoading(true);
       setSeekerFillWidth(0);
diff --git a/node_modules/@8man/react-native-media-console/src/VideoPlayer.tsx b/node_modules/@8man/react-native-media-console/src/VideoPlayer.tsx
index 1b57781..036d67d 100644
--- a/node_modules/@8man/react-native-media-console/src/VideoPlayer.tsx
+++ b/node_modules/@8man/react-native-media-console/src/VideoPlayer.tsx
@@ -114,6 +114,7 @@ const AnimatedVideoPlayer = (
   const [buffering, setBuffering] = useState(false);
   const [cachedDuration, setCachedDuration] = useState(0);
   const [cachedPosition, setCachedPosition] = useState(0);
+  const endedRef = useRef(false);
 
   const videoRef = props.videoRef || _videoRef;
 
@@ -178,6 +179,7 @@ const AnimatedVideoPlayer = (
   );
 
   const _onEnd = useCallback(() => {
+    endedRef.current = true;
     if (currentTime < duration) {
       setCurrentTime(duration);
       setPaused(!props.repeat);
@@ -192,6 +194,16 @@ const AnimatedVideoPlayer = (
     }
   }, [currentTime, duration, props.repeat, showOnEnd, onEnd]);
 
+  const isNearEnd = useCallback(
+    (time: number, totalDuration: number) => {
+      if (!(totalDuration > 0)) {
+        return false;
+      }
+      return time >= totalDuration - 0.25;
+    },
+    [],
+  );
+
   const _onError = useCallback(() => {
     setError(true);
     setLoading(false);
@@ -210,6 +222,7 @@ const AnimatedVideoPlayer = (
 
   const _onLoad = useCallback(
     (data: OnLoadData) => {
+      endedRef.current = false;
       setDuration(data.duration);
       setLoading(false);
 
@@ -294,12 +307,12 @@ const AnimatedVideoPlayer = (
         setTimeout(() => {
           !buffering && setPaused(true);
         });
-      } else {
+      } else if (!endedRef.current && !isNearEnd(currentTime, duration)) {
         setPaused(false);
       }
       console.log(playBack);
     },
-    [buffering],
+    [buffering, currentTime, duration, isNearEnd],
   );
 
   const events = useMemo(
@@ -438,10 +451,10 @@ const AnimatedVideoPlayer = (
   });
 
   useEffect(() => {
-    if (currentTime >= duration && duration > 0) {
-      videoRef?.current?.seek(0);
+    if (!isNearEnd(currentTime, duration) && endedRef.current) {
+      endedRef.current = false;
     }
-  }, [currentTime, duration, videoRef]);
+  }, [currentTime, duration, isNearEnd]);
 
   useEffect(() => {
     if (toggleResizeModeOnFullscreen) {
@@ -629,10 +642,10 @@ const AnimatedVideoPlayer = (
   // Memoize onBuffer callback
   const onBuffer = useCallback((e: {isBuffering: boolean}) => {
     setBuffering(e.isBuffering);
-    if (!e.isBuffering) {
+    if (!e.isBuffering && !endedRef.current && !isNearEnd(currentTime, duration)) {
       setPaused(false);
     }
-  }, []);
+  }, [currentTime, duration, isNearEnd]);
 
   // Memoize source URI for dependency comparison - use deep comparison for stability
   const sourceUri = useMemo(() => {
@@ -658,6 +671,7 @@ const AnimatedVideoPlayer = (
       sourceUri !== prevSourceUri.current &&
       sourceUri !== null
     ) {
+      endedRef.current = false;
       prevSourceUri.current = sourceUri;
       setLoading(true);
       setSeekerFillWidth(0);
